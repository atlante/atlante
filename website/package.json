{
  "name": "@atlante/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "bun run sync:brand && astro dev --port 4321",
    "build": "bun run sync:brand && bun run sync:schema && astro build",
    "check": "bun run build && ATLANTE_BUILT_OUTPUT_TESTS=1 bun test scripts/website-output.test.ts",
    "preview": "astro preview",
    "sync:brand": "bun scripts/sync-brand.ts",
    "sync:schema": "bun scripts/sync-schema.ts",
    "astro": "astro"
  },
  "dependencies": {
    "atlante": "0.2.2",
    "codejar": "4.3.0"
  },
  "devDependencies": {
    "astro": "^5.0.0"
  }
}
