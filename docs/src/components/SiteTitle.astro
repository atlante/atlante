---
import logoLight from "../assets/atlante-horizontal.svg";
import logoDark from "../assets/atlante-horizontal-reverse.svg";

const { siteTitle, siteTitleHref } = Astro.locals.starlightRoute;
---

<a href={siteTitleHref} class="site-title" aria-label={siteTitle} transition:persist="site-title">
  <img
    class="logo logo-light"
    src={logoLight.src}
    alt=""
    width={1200}
    height={420}
    loading="eager"
  />
  <img
    class="logo logo-dark"
    src={logoDark.src}
    alt=""
    width={1200}
    height={420}
    loading="eager"
  />
</a>

<style>
  @layer starlight.core {
    .site-title {
      display: flex;
      align-items: center;
      gap: var(--sl-nav-gap);
      width: clamp(6.75rem, 10vw, 8.5rem);
      min-width: 0;
      color: var(--sl-color-text-accent);
      text-decoration: none;
      white-space: nowrap;
    }

    .logo {
      display: block;
      height: calc(var(--sl-nav-height) - 2 * var(--sl-nav-pad-y));
      width: auto;
      max-width: 100%;
      object-fit: contain;
      object-position: 0 50%;
    }

    .logo-dark {
      display: none;
    }

    :global([data-theme="dark"]) .logo-light {
      display: none;
    }

    :global([data-theme="dark"]) .logo-dark {
