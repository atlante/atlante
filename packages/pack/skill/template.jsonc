{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Structured project skill with an optional title and overview, and ordered content sections. Title and overview render only when provided; OpenCode itself only consumes the binding description.",
  "type": "object",
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "overview": { "type": "string", "minLength": 1 },
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "oneOf": [
          {
            "properties": { "markdown": { "template": "@atlante/pack/markdown" } },
            "required": ["markdown"],
            "additionalProperties": false
          },
          {
            "properties": {
              "instructions": { "template": "@atlante/pack/instructions" }
            },
            "required": ["instructions"],
            "additionalProperties": false
          },
          {
            "properties": {
              "responsibilities": {
                "template": "@atlante/pack/responsibilities"
              }
            },
            "required": ["responsibilities"],
            "additionalProperties": false
          },
          {
            "properties": { "gotchas": { "template": "@atlante/pack/gotchas" } },
            "required": ["gotchas"],
            "additionalProperties": false
          },
          {
            "properties": { "workflow": { "template": "@atlante/pack/workflow" } },
            "required": ["workflow"],
            "additionalProperties": false
          },
          {
            "properties": {
              "invariants": { "template": "@atlante/pack/invariants" }
            },
            "required": ["invariants"],
            "additionalProperties": false
          },
          {
            "properties": {
              "references": { "template": "@atlante/pack/references" }
            },
            "required": ["references"],
            "additionalProperties": false
          }
        ]
      },
      "minItems": 1
    }
  },
  "required": ["sections"],
  "additionalProperties": false
}
