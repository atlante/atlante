{
  "$schema": "https://atlante.sh/schema/v0.1/eval-scenario.json",
  "version": "0.1",
  "name": "harness-red-green",
  "description": "Atlante agent fixes a red function inside the build workflow without touching anything else.",
  "task": {
    "fixture": "eval/fixtures/calculator",
    "agent": "atlante",
    "prompt": "src/sum.ts has a bug: sum(2, 3) currently returns -1, and sum(10, -4) currently returns 14, while verify.ts expects 5 and 6. Fix the implementation in src/sum.ts so bun verify.ts passes, and do not modify or create any other file."
  },
  "checks": [
    { "type": "command", "run": ["bun", "verify.ts"], "expectExit": 0 },
    { "type": "diff-allowlist", "allow": ["src/sum.ts"] }
  ]
}
